import { DataFetchProvider } from "../common/interfaces.js";
import { Flow, FlowConnect, SerializedFlow } from "../flow-connect";

export const deSerialize = (data: SerializedFlow, flowConnect: FlowConnect, receive?: DataFetchProvider): Promise<Flow> => {
  return null
}
